<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="@dimen/general_margin_size"
    android:orientation="vertical"
    tools:context=".MainActivity">

    <FrameLayout
        android:id="@+id/fm_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="100dp"
        android:layout_weight="1" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="bottom"
        android:layout_weight="3"
        android:orientation="vertical">

        <Button
            android:id="@+id/to_second"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/general_margin_size"
            android:layout_marginTop="@dimen/general_margin_size"
            android:layout_marginEnd="@dimen/general_margin_size"
            android:gravity="bottom"
            android:text="Second" />

        <Button
            android:id="@+id/to_option"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/general_margin_size"
            android:layout_marginTop="@dimen/general_margin_size"
            android:layout_marginEnd="@dimen/general_margin_size"
            android:gravity="bottom"
            android:text="Option Listener" />
    </LinearLayout>

</LinearLayout>